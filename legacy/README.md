# Arquivos legados (não usados no build atual)

Esta pasta guarda arquivos da configuração antiga do projeto (ex.: CRA/Craco e scripts de build antigos). Eles ficam aqui apenas por histórico e referência. O site atual é construído na pasta `soleil-frontend` usando Vite.

Não são usados no build/deploy atual:
- `components.json`
- `craco.config.js`
- `js.config.json`
- `packege.json` (nome incorreto; sobrou de uma estrutura antiga)
- `postcss.config.js`
- `tailwind.config.js` (da raiz; o Tailwind ativo é o de `soleil-frontend/tailwind.config.js`)

Se em algum momento você quiser simplificar ainda mais, pode excluir estes arquivos — não impacta o site publicado.
